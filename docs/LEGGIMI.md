# docs

- `progetto/`: documenti di lavoro del progetto, copiati il 23/09/2026 per la revisione. Ogni documento porta la propria data di aggiornamento; i più vecchi descrivono lo stato del lavoro a quella data (per esempio la specifica v0.2 e le note del passo C). Lo stato corrente dei modelli e dei test è in `progetto/09_registro_modelli_e_test.md`.
- `rapporto_M71-E6-predittivo.md` e `grafici_predittivo/`: rapporto e grafici dell'esecuzione ufficiale `20260923-072127_M71-E6-predittivo`.
- Il pacchetto dati per la revisione è nella cartella `condivisione/` alla radice del repository (vedi il suo README).
